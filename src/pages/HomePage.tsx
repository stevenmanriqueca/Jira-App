import { FC } from 'react';
import { HomeContainer } from '../containers';

const HomePage: FC = () => {
    return (
        <HomeContainer>
            <div>HomePage</div>
        </HomeContainer>
    )
}

export default HomePage